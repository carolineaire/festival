<div class="container-fluid bg-body-tertiary azy">
  <div class="container">
    <div class="row">
      <div class="col-md-2"></div>
      <div class="col-md-7">
      
        <nav class="navbar navbar-expand-lg">
          <div class="container-fluid">
            <a class="navbar-brand" href="{{path('app_post')}}">DEMOS</a>&emsp;&emsp;
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                  <a class="nav-link active" aria-current="page"  href="{{path('app_post')}}">
                    <i class="fa-solid fa-house" style="color:black;"></i>
                  </a>
                </li>
                <li class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    Voyage
                  </a>
                  <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="#">A la une</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="#">Tous les articles</a></li>
                  </ul>
                </li>
                <li class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    Politique
                  </a>
                  <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="#">A la une</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="#">Tous les articles</a></li>
                  </ul>
                </li>
                <li class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    Sport
                  </a>
                  <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="#">A la une</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="#">Tous les articles</a></li>
                  </ul>
                </li>
                <li class="nav-item dropdown">
                  <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                    Divers
                  </a>
                  <ul class="dropdown-menu">
                    <li><a class="dropdown-item" href="#">A la une</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="#">Tous les articles</a></li>
                  </ul>
                </li>
                {#<li class="nav-item">
                  <a class="nav-link" href="#">Sponsors</a>
                </li>#}
              </ul>&emsp;&emsp;

              {# <div class="header-social-links d-flex">
                {% if app.user %}
                  <a class="col-md-12 dropbtn" href="{{path('app_logout')}}">
                    {% if app.user.avatar is empty %}
                      <i class="fa-solid fa-circle-user"></i>
                    {% elseif app.user.opption is same as ('yes') %}
                      <img class="roundedImage" src="{{asset('divers/avatars/' ~ app.user.avatar)}}" style="width:25px; height:25px; border-radius:50%" alt="">&emsp;
                      <span class="zxfirstname">{{ app.user.firstname|capitalize }}</span> <span class="zxlastname">{{ app.user.lastname|upper }}</span>
                      coller le power of #}
                      {# <a class="loggouts" href="{{path('app_logout')}}">
                        <i class="fa fa-sign-out" aria-hidden="true"></i>
                      </a> #}
                      {# lien vers dashborad #}
                      {# <a class="zxadmin" href="{{path('admin')}}">
                        <i class="fa-solid fa-circle-play"></i>
                      </a>
                    {% else %}
                      <img class="roundedImage" src="{{asset('divers/avatars/' ~ app.user.avatar)}}" style="width:25px; height:25px; border-radius:50%" alt="">&emsp;
                      <span class="zxfirstname">{{ app.user.firstname|capitalize }}</span> <span class="zxlastname">{{ app.user.lastname|upper }}</span>
                      coller le power of #}
                      {# <a class="loggouts" href="{{path('app_logout')}}">
                        <i class="fa fa-sign-out" aria-hidden="true"></i>
                      </a>
                    {% endif %}
                  </a>
                {% else %}
                  <a class="mylog" href="{{path('app_login')}}">
                    <i class="fa-regular fa-circle-user"></i>
                  </a>
                {% endif %}
              </div> #}
              




              {# <div class="header-social-links d-flex">
                {% if not app.user %} 
                  <a class"zxuser" href="{{path('app_login')}}">
                    <i class="far fa-user-circle"></i>
                  </a>
                {% else %}
                  <h6 class="" style="width: 180px; font-size: 0.8em; color: grey; cursor: pointer; margin-left: 12px;">
                    <div class="col-md-12">
                      @{{app.user.pseudo}}&emsp;
                      <img class="roundedImage" src="{{asset('divers/avatars/' ~ app.user.avatar)}}" alt="{{app.user.pseudo}}">
                    </div>
                  </h6>
                {% endif %}
                <div class="header-social-links d-flex">
                  <div class="col-md-2">
                    {% if not app.user %}
                    {% else %}
                      <a class="loggouts" href="{{path('app_logout')}}">
                        <i class="fa fa-sign-out" aria-hidden="true"></i>
                      </a>
                    {% endif %}
                  </div>
                </div>
              </div> #}
              
            </div>
          </div>
        </nav>

      </div>
    </div>
  </div>
</div>